<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pace.cs3892018team5.dev.chinesefortuneaide">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:background="#CD3C3C"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main_Screen" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Quiz2_Screen"
            android:label="@string/title_activity_quiz2__screen"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Quiz1_Screen"
            android:label="@string/title_activity_quiz1__screen"
            android:theme="@style/AppTheme" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".ResultActivity2" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"></activity>
    </application>

</manifest>